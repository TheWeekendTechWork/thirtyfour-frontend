import { Box } from "@chakra-ui/react";

export const HeroSection: React.FC = () => {
    return (
        <Box bg="retroGreen.900" py={20} backgroundImage="...">
            {/* Hero section content */}
        </Box>
    );
};
